import React from "react"; import { Connection } from "../types/goal.types"; interface ConnectionLinesProps { connections: Connection[]; canvasSize: { width: number; height: number }; } export const ConnectionLines: React.FC<ConnectionLinesProps> = ({ connections, canvasSize }) => { return ( <svg className="absolute top-0 left-0 w-full h-full pointer-events-none z-0" style={{ width: canvasSize.width, height: canvasSize.height }} > {connections.map(conn => ( <g key={conn.id}> <path d={conn.path} stroke={conn.completed ? "#10b981" : "#6b7280"} strokeWidth="3" fill="none" className="transition-all duration-300" strokeDasharray={conn.completed ? "0" : "5,5"} /> <circle cx={conn.from.x} cy={conn.from.y} r="4" fill={conn.completed ? "#10b981" : "#6b7280"} /> <circle cx={conn.to.x} cy={conn.to.y} r="4" fill={conn.completed ? "#10b981" : "#6b7280"} /> </g> ))} </svg> ); };
