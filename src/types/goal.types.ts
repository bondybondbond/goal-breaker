export interface Goal { id: number; text: string; completed: boolean; level: number; gridRow: number; parentId: number | null; position: Position; isEditing?: boolean; priority?: 'high' | 'medium' | 'low'; children: Goal[]; } export interface Position { x: number; y: number; } export interface Connection { id: string; from: Position; to: Position; completed: boolean; path: string; type?: 'normal' | 'placeholder'; parentId?: number; hiddenCount?: number; } export interface GridConfig { COLUMN_WIDTH: number; ROW_HEIGHT: number; CARD_WIDTH: number; CARD_HEIGHT: number; MARGIN: number; VERTICAL_GAP: number; } export interface CanvasState { offset: Position; isPanning: boolean; panStart: Position; size: { width: number; height: number; }; } export interface DragState { draggedGoal: Goal | null; dragOffset: Position; } export interface LevelStyle { border: string; borderWidth: string; bg: string; color: string; } export interface LevelStats { total: number; completed: number; }
